# GA Multi MCP - Environment Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# REQUIRED: Google Service Account Credentials
# =============================================================================

# Path to your Google Cloud service account JSON file
# This account must have:
# - Analytics API enabled in Google Cloud
# - Viewer access to your GA4 properties
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account.json

# Alternative environment variable names (for compatibility)
# GA_CREDENTIALS_PATH=/path/to/your/service-account.json
# GA_SERVICE_ACCOUNT_PATH=/path/to/your/service-account.json

# =============================================================================
# OPTIONAL: Cache Configuration
# =============================================================================

# Cache TTL for API responses in seconds (default: 300 = 5 minutes)
# GA_CACHE_TTL=300

# Cache TTL for property list in seconds (default: 3600 = 1 hour)
# GA_PROPERTY_CACHE_TTL=3600

# =============================================================================
# OPTIONAL: Query Configuration
# =============================================================================

# Default limit for query results (default: 1000)
# GA_DEFAULT_LIMIT=1000

# =============================================================================
# OPTIONAL: Fuzzy Matching Configuration
# =============================================================================

# Fuzzy matching threshold for property names (0.0 to 1.0, default: 0.6)
# Lower values = more lenient matching, higher = stricter
# GA_FUZZY_THRESHOLD=0.6

# Custom property aliases as JSON object
# Maps property names to list of aliases that should resolve to them
# Example: {"myblog": ["blog", "personal site"], "myapp": ["app", "mobile"]}
# GA_PROPERTY_ALIASES={}

# =============================================================================
# OPTIONAL: Error Handling
# =============================================================================

# Whether to mask detailed error messages (default: false)
# Set to true in production to hide implementation details
# GA_MASK_ERRORS=false
